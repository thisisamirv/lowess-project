% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{finalize}
\alias{finalize}
\title{Finalize a streaming LOWESS model}
\usage{
finalize(model, ...)
}
\arguments{
\item{model}{A \code{StreamingLowess} object.}

\item{...}{Not used.}
}
\value{
A \code{LowessResult} combining all processed chunks.
}
\description{
Finalize a streaming LOWESS model
}
\examples{
x <- seq(0, 10, length.out = 100)
y <- sin(x) + rnorm(100, 0, 0.1)
model <- StreamingLowess(fraction = 0.2, chunk_size = 50L)
invisible(process_chunk(model, x[1:50], y[1:50]))
final <- finalize(model)
}
