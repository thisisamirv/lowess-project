% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{fit}
\alias{fit}
\title{Fit a LOWESS model to data}
\usage{
fit(model, ...)
}
\arguments{
\item{model}{A \code{Lowess} object.}

\item{...}{Not used.}

\item{x}{Numeric vector of predictor values.}

\item{y}{Numeric vector of response values.}

\item{custom_weights}{Optional numeric vector of non-negative per-observation
weights. \code{NULL} (default) applies no custom weighting.}
}
\value{
A \code{LowessResult} object.
}
\description{
Fit a LOWESS model to data
}
\examples{
x <- seq(0, 10, length.out = 100)
y <- sin(x) + rnorm(100, 0, 0.1)
model <- Lowess(fraction = 0.2)
result <- fit(model, x, y)
}
