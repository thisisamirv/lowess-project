% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Lowess.R
\name{cv_opts}
\alias{cv_opts}
\title{Cross-validation options for \code{\link{Lowess}}}
\usage{
cv_opts(fractions, method = "kfold", k = 5L, seed = NULL)
}
\arguments{
\item{fractions}{Numeric vector of candidate smoothing fractions, each
greater than 0 and up to 1 (e.g. \code{c(0.2, 0.3, 0.5)}).}

\item{method}{Cross-validation method: \code{"kfold"} (default) or
\code{"loocv"}.}

\item{k}{Number of folds for k-fold cross-validation. Default: 5.}

\item{seed}{Integer seed for reproducible fold assignment. \code{NULL}
(default) uses a random seed.}
}
\value{
A \code{cv_opts} list for \code{Lowess(cv = ...)}.
}
\description{
Build a cross-validation options list to pass to
\code{Lowess(cv = ...)}. Cross-validation is disabled by default
(\code{cv = NULL}); supplying \code{cv_opts()} enables it and lets the
model pick the best smoothing fraction from the candidates.
}
\examples{
model <- Lowess(cv = cv_opts(fractions = c(0.2, 0.3, 0.5)))
}
