% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{predict.Lowess}
\alias{predict.Lowess}
\title{Predict from a fitted LOWESS model at out-of-sample points}
\usage{
\method{predict}{Lowess}(
  object,
  new_x,
  return_se = FALSE,
  confidence_level = NULL,
  prediction_level = NULL,
  return_derivative = FALSE,
  extrapolation = "clamp",
  max_extrapolation_distance = NULL,
  max_neighbor_distance = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{Lowess} object, fitted (via \code{\link{fit}}) with
\code{retain_model = TRUE} passed to \code{\link{Lowess}}.}

\item{new_x}{Numeric vector of out-of-sample query points.}

\item{return_se}{Logical; include standard errors in the output. Default:
\code{FALSE}.}

\item{confidence_level}{Confidence interval coverage level (e.g. 0.95).
\code{NULL} (default) disables it.}

\item{prediction_level}{Prediction interval coverage level (e.g. 0.95).
\code{NULL} (default) disables it.}

\item{return_derivative}{Logical; include the local fit's derivative
(slope) in the output. Default: \code{FALSE}.}

\item{extrapolation}{Behavior for query points outside the training range:
\code{"clamp"} (default), \code{"linear"}, or \code{"error"}.}

\item{max_extrapolation_distance}{Under \code{"linear"} extrapolation, the
maximum allowed distance beyond the training boundary before
\code{predict} errors instead of returning an unbounded value.
\code{NULL} (default) disables the cap.}

\item{max_neighbor_distance}{Maximum allowed distance to the farthest point
in a query's local window before \code{predict} errors, catching
in-range-but-sparse query points. \code{NULL} (default) disables the cap.}

\item{...}{Must be empty.}
}
\value{
A list with a \code{y} element (predicted values) and optional
\code{standard_errors}/\code{confidence_lower}/\code{confidence_upper}/
\code{prediction_lower}/\code{prediction_upper}/\code{derivative} elements.
}
\description{
Predict from a fitted LOWESS model at out-of-sample points
}
\examples{
x <- seq(0, 10, length.out = 100)
y <- sin(x) + rnorm(100, 0, 0.1)
model <- Lowess(fraction = 0.2, retain_model = TRUE)
fit(model, x, y)
predict(model, c(2.5, 7.5))
}
