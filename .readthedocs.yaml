# ReadTheDocs configuration for MkDocs
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

mkdocs:
  configuration: bindings/python/docs/mkdocs.yml

python:
  install:
    - requirements: bindings/python/docs/requirements.txt
