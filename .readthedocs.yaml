# ReadTheDocs configuration for Sphinx
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

sphinx:
  configuration: bindings/python/docs/conf.py

python:
  install:
    - requirements: bindings/python/docs/requirements.txt
